# [프로젝트 이름] 서비스 기획서 (PRD)

- 작성자: [이름]
- 작성일: (오늘 날짜)

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

우리 부서의 여러 취합 담당자는 담당자들이 각자 다르게 작성해 hwpx(한글) 또는 PDF 형식으로 제출한 문서를, 부서가 처음 배포한 양식(폰트, 참고 박스 색깔 등 서식)에 맞춰 정리해야 한다. 문서를 작성해서 보내는 사람은 이 앱을 쓰지 않고, 취합 담당자만 사용한다. 문서마다 서식이 제각각이라 하나하나 열어 서식(폰트, 참고 박스 색깔 등)을 맞추고 정리하는 수작업에 많은 시간이 들어가는 것이 문제다.

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

현재는 취합 담당자가 제출받은 hwpx·PDF 문서를 하나씩 직접 열어 폰트, 글자크기, 글머리 기호, 제목 띠지 등을 원본 양식에 맞게 수작업으로 고치고, 이를 다시 하나의 파일로 수동으로 합친 뒤 목차도 직접 만들고 있다. 문서 수가 많아질수록 시간이 오래 걸리고, 사람이 직접 맞추다 보니 서식이 빠지거나 담당자마다 결과물이 조금씩 달라지는 한계가 있다.

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 문서 서식 통일 + 병합 + 목차 생성에 걸리는 시간을 기존 수작업 대비 50% 이상 단축
- 병합된 최종 파일에서 서식(폰트·글자크기·글머리 기호·제목 띠지) 불일치 오류 0건
- 목차의 제목·페이지 번호 누락 또는 오인식 0건

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

- 사용자: 우리 부서의 취합 담당자 (문서 작성자는 이 앱을 쓰지 않음)
- 이용 흐름: 담당자가 hwpx·PDF 파일 업로드 → 서식 기준이 될 샘플 문서 등록(학습) → 서식 자동 통일(hwpx) 및 파일 병합(hwpx+PDF) → 페이지 번호 부여 → 제목 스타일 기반 목차 자동 생성 → 완성된 하나의 파일 다운로드

## 5. 주요 기능 (must / nice로 구분)

### Must

**1) hwpx 서식 자동 맞춤 및 파일 병합**
- 설명: 담당자들이 제출한 hwpx 문서는 폰트, 글자크기, 글머리 기호, 제목 띠지를 원본 양식 기준으로 자동 통일하고, PDF 문서는 서식 변경 없이 내용을 그대로 유지한 채, hwpx·PDF 파일 전체를 하나의 파일로 자동 병합
- AI가 꼭 지켜야 할 규칙:
  - 본문 텍스트 내용은 절대 수정·삭제하지 않는다
  - 서식 통일은 hwpx 파일에만 적용한다 (폰트 종류·글자크기·글머리 기호·제목 띠지를 원본 양식 기준값으로 맞춤)
  - PDF 파일은 서식을 변경하지 않고 원본 내용 그대로 병합 대상에 포함한다
  - 서식 기준(폰트, 글자크기 pt 값 등)은 고정값으로 미리 정해두지 않고, 작업할 때마다 사용자가 제공하는 샘플 문서를 학습해서 그 기준에 맞춰 적용한다
  - 병합된 여러 hwpx·PDF 파일은 최종적으로 하나의 파일로 합친다
  - 파일은 취합(업로드)된 순서대로 병합하고, 그 순서에 따라 페이지 번호를 매긴다

**2) 목차 자동 생성**
- 설명: 각 hwpx·PDF 파일에서 일정한 글자크기·폰트(제목 스타일)가 적용된 텍스트를 제목으로 인식해, 페이지 번호와 함께 하나의 목차로 자동 생성 (여러 줄에 걸쳐 있어도 인식)
- AI가 꼭 지켜야 할 규칙:
  - 제목은 위치(첫 줄 여부)와 상관없이, 지정된 제목 스타일(글자크기·폰트)이 적용된 텍스트만 인식한다 (그 외 텍스트는 제목으로 오인하지 않음)
  - 여러 줄에 걸쳐 같은 스타일이 이어지면 하나의 제목으로 합쳐서 인식한다
  - 목차는 문서가 취합된 순서대로 정렬하고, 제목 텍스트는 원문 그대로 가져온다
  - 목차의 각 제목 옆에는 병합된 파일 기준 해당 제목의 페이지 번호를 함께 표시한다
  - 취합된 문서 개수에 제한을 두지 않고 전부 목차에 표시한다

### Nice to have
- 해당 없음 (이번 범위에서는 Must 2개 기능 외 추가 기능을 두지 않음 — 6번 비범위 참고)

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

### 범위 (만들 것)
- hwpx 서식 자동 맞춤 및 파일 병합
- 목차 자동 생성

### 비범위 (만들지 않을 것)
- 문서 작성자(제출자)를 위한 별도 화면/기능은 만들지 않는다 (취합 담당자 전용 도구)
- hwpx, PDF 외의 파일 형식 지원은 만들지 않는다
- 문서 내용(텍스트)을 임의로 추가하거나 삭제하는 기능은 절대 만들지 않는다 (양식/서식만 수정)
- 사용자 계정/로그인, 여러 부서 간 권한 관리 기능은 만들지 않는다

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 공개등급: 취합 대상 문서는 부서 내부 업무 문서로, 외부 비공개를 원칙으로 한다
- 개인정보 여부: 문서 내용에 개인정보가 포함될 가능성이 있으므로, 문서 내용은 절대 수정·저장 목적으로 가공하지 않고 서식 통일·병합·목차 생성 용도로만 처리한다
- API 키 관리: `GITHUB_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`, `OPENAI_API_KEY` 등은 `.env`에만 보관하고 화면·로그·커밋에 노출하지 않으며, `.gitignore`에 등록된 상태를 유지한다

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- Next.js — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- 고정 이유: STEP 2에서 만든 뼈대와 같은 구성이고, STEP 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함
