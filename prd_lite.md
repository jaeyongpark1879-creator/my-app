# [프로젝트 이름] 서비스 기획서 (PRD_lite)

- 작성자: [이름]
- 작성일: (오늘 날짜)

---

## 1. 이 앱은 한 마디로 무엇인가요?
- 답변: 여러 사람이 hwpx(한글) 또는 PDF 형식으로 제출한 문서를, 우리 부서가 처음 배포한 양식(폰트, 참고 박스 색깔 등 서식)에 맞춰 정리한 뒤 하나의 파일로 자동 합쳐주는 앱

---

## 2. 누가 쓰고, 왜 쓰나요? (딱 한 줄씩만!)
- 누가 쓰나요?
  - 답변: 우리 부서의 여러 취합 담당자 (문서를 작성해서 보내는 사람은 앱을 쓰지 않고, 취합 담당자만 사용)
- 어떤 불편을 해결하나요?
  - 답변: 각자 다르게 작성해서 받은 hwpx 문서들을 하나하나 열어 서식(폰트, 참고 박스 색깔 등)을 맞추고 정리하던 수작업을 줄여줌

---

## 3. 개발할 핵심 기능 (딱 2가지만!)
> 💡 너무 많은 기능을 만들려고 하면 AI가 코드를 꼬아버립니다.
> 가장 중요한 2개 기능만 "규칙"을 정해 알려주세요.

### 1) hwpx 서식 자동 맞춤 및 파일 병합
- 설명: 담당자들이 제출한 hwpx 문서는 폰트, 글자크기, 글머리 기호, 제목 띠지를 원본 양식 기준으로 자동 통일하고, PDF 문서는 서식 변경 없이 내용을 그대로 유지한 채, hwpx·PDF 파일 전체를 하나의 파일로 자동 병합
- AI가 꼭 지켜야 할 규칙:
  - 본문 텍스트 내용은 절대 수정·삭제하지 않는다
  - 서식 통일은 hwpx 파일에만 적용한다 (폰트 종류·글자크기·글머리 기호·제목 띠지를 원본 양식 기준값으로 맞춤)
  - PDF 파일은 서식을 변경하지 않고 원본 내용 그대로 병합 대상에 포함한다
  - 서식 기준(폰트, 글자크기 pt 값 등)은 고정값으로 미리 정해두지 않고, 작업할 때마다 사용자가 제공하는 샘플 문서를 학습해서 그 기준에 맞춰 적용한다
  - 병합된 여러 hwpx·PDF 파일은 최종적으로 하나의 파일로 합친다
  - 파일은 취합(업로드)된 순서대로 병합하고, 그 순서에 따라 페이지 번호를 매긴다

### 2) 목차 자동 생성
- 설명: 각 hwpx·PDF 파일에서 일정한 글자크기·폰트(제목 스타일)가 적용된 텍스트를 제목으로 인식해, 페이지 번호와 함께 하나의 목차로 자동 생성 (여러 줄에 걸쳐 있어도 인식)
- AI가 꼭 지켜야 할 규칙:
  - 제목은 위치(첫 줄 여부)와 상관없이, 지정된 제목 스타일(글자크기·폰트)이 적용된 텍스트만 인식한다 (그 외 텍스트는 제목으로 오인하지 않음)
  - 여러 줄에 걸쳐 같은 스타일이 이어지면 하나의 제목으로 합쳐서 인식한다
  - 목차는 문서가 취합된 순서대로 정렬하고, 제목 텍스트는 원문 그대로 가져온다
  - 목차의 각 제목 옆에는 병합된 파일 기준 해당 제목의 페이지 번호를 함께 표시한다
  - 취합된 문서 개수에 제한을 두지 않고 전부 목차에 표시한다

---

## 4. 이번에 절대 만들지 않을 기능 (욕심 버리기)
> 💡 AI에게 미리 "이건 안 만들 거야"라고 선언해야 엉뚱한 코딩을 안 합니다.
- 문서 작성자(제출자)를 위한 별도 화면/기능은 만들지 않는다 (취합 담당자 전용 도구)
- hwpx, PDF 외의 파일 형식 지원은 만들지 않는다
- 문서 내용(텍스트)을 임의로 추가하거나 삭제하는 기능은 절대 만들지 않는다 (양식/서식만 수정)
- 사용자 계정/로그인, 여러 부서 간 권한 관리 기능은 만들지 않는다

---

## 5. 디자인 느낌과 색상 고르기
- 전체적인 분위기: 깔끔하고 신뢰감 있는 관공서/업무용 느낌 (군더더기 없이 담백하게)
- 메인 색상: 기획예산처 로고와 어울리는 색상 (남색/파란색 계열)
- 화면 크기 제약: 데스크톱(PC) 화면 기준, 모바일 대응은 고려하지 않음
